#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)

{
	char alb;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(alb[i]);
	}
	putchar('\n');

	return (0);
}
