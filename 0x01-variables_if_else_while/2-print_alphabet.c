#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)

{
	char alb;

	for (alb = 'a'; alb <= 'z'; alb++)
	{
		putchar(alb);
	}
	putchar("\n");

	return (0);
}
